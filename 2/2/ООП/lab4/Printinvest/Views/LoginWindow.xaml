<Window x:Class="Printinvest.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource WindowTitle}"
        Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{DynamicResource UsernameLabel}" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" />
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Style="{StaticResource TextBoxStyle}" />

        <TextBlock Text="{DynamicResource PasswordLabel}" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,10,0" />
        <PasswordBox Grid.Row="1" Grid.Column="1" Margin="0,10,0,0"
                     PasswordChanged="PasswordBox_PasswordChanged" />

        <Button Content="{DynamicResource LoginButton}" Grid.Row="2" Grid.ColumnSpan="2" Margin="0,20,0,0"
                Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" />
    </Grid>
</Window>